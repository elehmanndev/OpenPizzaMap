node_modules
.env
.env.local
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.DS_Store
.prisma
*.log
import-errors.json
import-log*.txt
.claude/
.playwright-mcp/

# Place images: committed to git so they survive Hostinger's deploy-cleans.
# Generated by scripts/download-images.js (run on server, scp'd into the repo).
# Net ~200MB binary. Acceptable for now — refactor to env-var path next session if it bothers us.
scripts/lib/.tripadvisor-budget.json
