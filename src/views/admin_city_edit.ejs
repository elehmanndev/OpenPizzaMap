<%- include("layout", { title: `Admin · City · ${city.name}`, user, leaflet: false, body: String.raw`
  <h1>Edit city: ${escapeHtml(city.name)}</h1>
  <div class="small">Country: ${escapeHtml(city.countryCode)} · Slug: ${escapeHtml(city.slug)}</div>
  <form method="post" action="/admin/cities/${city.id}" class="card" style="display:grid; gap:12px; margin-top:12px;">
    <label>
      <div class="small">Name</div>
      <input name="name" value="${escapeAttr(city.name || "")}" required />
    </label>
    <label>
      <div class="small">Slug</div>
      <input name="slug" value="${escapeAttr(city.slug || "")}" required />
    </label>
    <label>
      <div class="small">Hero image URL</div>
      <input name="heroImageUrl" value="${escapeAttr(city.heroImageUrl || "")}" placeholder="https://..." />
    </label>
    <label>
      <div class="small">Visible</div>
      <input type="checkbox" name="isVisible" value="1" ${city.isVisible ? "checked" : ""} />
    </label>
    <div>
      <div class="small">Intro</div>
      <div id="editor" style="background: var(--c-surface);"></div>
      <input type="hidden" name="introHtml" id="introHtml" />
    </div>
    <div class="row">
      <div style="flex:1;">
        <button type="submit">Save</button>
      </div>
      <div style="flex:1;">
        <a class="btn" href="/admin/cities">Back</a>
      </div>
    </div>
  </form>

  <link href="https://cdn.quilljs.com/1.3.6/quill.snow.css" rel="stylesheet" />
  <script src="https://cdn.quilljs.com/1.3.6/quill.min.js"></script>
  <script>
    (function () {
      var editorEl = document.getElementById("editor");
      var input = document.getElementById("introHtml");
      var form = editorEl ? editorEl.closest("form") : null;
      if (!editorEl || !input || !form) return;

      var quill = new Quill(editorEl, {
        theme: "snow",
        modules: { toolbar: [["bold", "italic", "underline"], [{ list: "ordered" }, { list: "bullet" }], ["link"]] },
      });
      quill.root.innerHTML = ${JSON.stringify(city.introHtml || "")};

      form.addEventListener("submit", function () {
        input.value = quill.root.innerHTML || "";
      });
    })();
  </script>
` }) %>
